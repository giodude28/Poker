<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.poker">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_config"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".Mainhome"></activity>
        <activity android:name=".HomeDetails" />
        <activity android:name=".GuideDetail" />
        <activity android:name=".GuideList" />
        <activity android:name=".TipsStratDetails" />
        <activity android:name=".TipsStrategies" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".PlayerDetail"
            android:parentActivityName=".PlayerList"
            android:theme="@style/AppTheme1" />
        <activity android:name=".PlayerList" />
        <activity android:name=".CardRankDetails" />
        <activity android:name=".PokerPlayer" />
        <activity android:name=".CardsRank" />
        <activity
            android:name=".Splash"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>